# ✅ Synced with Codebase (2025-10-20)

Use the following implemented endpoints:
- `/api/analytics/sales-summary`, `/top-items`, `/hourly-sales`, `/customer-analytics`, `/kitchen-performance`, `/table-utilization`
- `/api/tables/analytics/*`, `/api/tables/advanced-analytics/*`
- `/api/analytics/export/{csv|json}`
- Inventory: `/api/inventory/stock-override-analytics`, summary `/api/analytics/summary`

Planned: dedicated ABC and profit analysis endpoints (pages may derive from existing data meanwhile).

See also: `common_invariants.md`, `capabilities_matrix.md`. 